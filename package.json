{
  "name": "kamil-kolodziejczyk-formic-studio",
  "private": true,
  "workspaces": [
    "web",
    "studio"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:studio": "npm run dev --workspace studio",
    "build": "npm run build --workspace web",
    "build:studio": "npm run build --workspace studio"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}

